version: "3"
services:
  chart:
    image: lanntxyz/chart-server-docker:latest
    ports:
      - 8080:80
    volumes:
      - ./:/app
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
    command: /app/script/start.sh
  redis:
    image: redis:latest
    volumes:
      - redis_data:/data

volumes:
  redis_data:
