version: "3"
services:
  chart:
    image: lanntxyz/chart-server-docker:latest
    ports:
      - 80:8080
    networks:
      - webnet
    volumes:
      - ./:/opt/chart
    environment:
      - REDIS_HOST=redis

networks:
  webnet:
