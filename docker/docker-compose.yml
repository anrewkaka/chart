version: "3"
services:
  chart:
    image: lanntxyz/chart:latest
    ports:
      - 80:8080
    environment:
      - REDIS_HOST=redis
    volumes:
      - ../build/libs:/opt/chart
      - /opt/chart/config:/opt/chart/resources
    entrypoint: java -jar -Dspring.config.location=/opt/chart/resources/application.yml /opt/chart/chart-0.0.1-SNAPSHOT.jar
  redis:
     image: redis:latest
     volumes:
        - redis_data:/data

volumes:
  redis_data:
